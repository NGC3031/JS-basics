//example of basic map function

function map(array) {
    return arr.map(Math.sqrt);
}

arr = [3, 4, 5, 6, 7];

console.log(map(arr));

//more complex mapping

function wordMap(array) {
    return arrWords.map(s => 'Police' + s);
}

arrWords = ['car', 'truck', 'motorcycle'];

console.log(wordMap(arrWords));