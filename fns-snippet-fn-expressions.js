// function declaration through function expressions
// we can assign the variable multiply the value of the anonymous function
var multiply = function (a, b) {
    return a * b;
}
c = multiply(3, 2);
console.log(c); // 6